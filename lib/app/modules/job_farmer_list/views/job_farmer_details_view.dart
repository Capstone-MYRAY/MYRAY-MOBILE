import 'package:flutter/material.dart';

class JobFarmerDetailsView extends StatelessWidget {
  const JobFarmerDetailsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
