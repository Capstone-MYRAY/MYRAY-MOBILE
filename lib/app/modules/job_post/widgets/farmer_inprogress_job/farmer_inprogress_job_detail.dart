
import 'package:flutter/material.dart';

class FarmerInProgressJobDetail extends StatelessWidget {
  const FarmerInProgressJobDetail({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Chi tiết'),
        centerTitle: true,
      ),
    );
  }
}